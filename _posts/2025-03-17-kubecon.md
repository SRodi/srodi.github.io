---
title: "Kubecon Diagrams"
date: 2026-03-17 09:00:00 +0100
categories: [kubernetes, networking]
tags: [kubernetes, networking, debugging, troubleshooting]
mermaid: true
---

## Slide 1

```mermaid
graph LR
    subgraph Node1 [Node 1]
        PodA[Pod A<br>10.244.1.5]
    end
    subgraph Node2 [Node 2]
        PodB[Pod B<br>10.244.2.5]
    end
    Packet("<div style='font-size: 13px;'>IP Packet</div><div style='font-size: 11px;'>src: 10.244.1.5<br>dst: 10.244.2.5</div>")
    PodA --- Packet --> PodB
    %% Highlight the packet flow
    linkStyle 0,1 stroke:#ff5555,stroke-width:4px;
    %% Style Packet
    style Packet fill:#f2f2f2,stroke:#d9534f,stroke-width:2px,color:#333
```

## Slide 4

```mermaid
flowchart TD
    subgraph Node1 [Node 1]
        PodA[Pod A]
        PodB[Pod B]
    end
    subgraph Node2 [Node 2]
        PodC[Pod C]
    end
    PodA <--> PodC
    PodB <--> PodC
    %% Highlight the packet flow
    linkStyle 0,1 stroke:#ff5555,stroke-width:4px;
```

## Slide 5

```mermaid
flowchart LR
    Kubelet[Kubelet]
    CNI[CNI Plugin]
    Runtime[Container Runtime]
    subgraph PodNS ["Pod Network Namespace"]
        direction TB
        Pause[pause container]
        App[app container]
        Pause ~~~ App
    end
    Kubelet --> Runtime
    Runtime -->|CNI ADD| CNI
    Runtime -->|"creates<br><small>(pause container -> netns)"| PodNS
    CNI -->|configures veth,<br>IP, routes| PodNS
```

## Slide 6old

```mermaid
%%{init: {'sequence': {'mirrorActors': false}}}%%
sequenceDiagram
    participant K as Kubelet
    participant CR as Container Runtime
    participant C as CNI Plugin
    participant P as Pod Network

    K->>CR: 1. Create Pod Sandbox
    rect rgba(255, 0, 0, 0.1)
    CR->>P: 2. Create netns (pause container)
    end
    CR->>C: 3. Call CNI ADD
    activate C
    rect rgba(255, 0, 0, 0.1)
    C->>P: 4a. Create veth pair
    C->>P: 4b. Assign IP address (via IPAM)
    C->>P: 4c. Setup routes
    end
    C-->>CR: 5. Return success
    deactivate C
    CR-->>K: 6. Sandbox ready
    Note over K,CR: pod has IP before containers start
    K->>CR: 7. Start containers (share netns)
```

## Slide 6

```mermaid
graph LR       
    subgraph HostNS ["Host netns"]
        NodeEth["Node gateway <br>(10.244.1.1)"]
        CNI_Mechanism["Node datapath<br>(iptables/eBPF)"]
        Veth_Host["veth (host-side)"]
    end

    subgraph PodNS ["Pod netns"]
        Eth0_Pod["veth (pod-side/eth0)"]
    end

    %% Wiring
    NodeEth <---> CNI_Mechanism
    CNI_Mechanism <---> Veth_Host
    Veth_Host <--> Eth0_Pod
    %% Highlight the packet flow
    linkStyle 0,1,2 stroke:#ff5555,stroke-width:4px;
```

## Slide 7 (Overlay vs Underlay)

```mermaid
graph TD
    subgraph Node_1[Node 1: 192.168.1.10]
        PodA[Pod A: 10.0.1.5]
    end

    subgraph Node_2[Node 2: 192.168.1.11]
        PodB[Pod B: 10.0.2.5]
    end

    PhysNet(Physical Network)

    %% Underlay Path
    PodA -->|"Underlay: native routing (Pod IP routable)<br>Packet = (Src: 10.0.1.5, Dst: 10.0.2.5)"| PhysNet
    PhysNet -->|Fast, No Encap| PodB

    %% Overlay Path
    Node_1 -.->|"Overlay: Tunnel Routing Node IPs<br>Packet = (Outer Src: 192.168.1.10, Dst: 192.168.1.11) (Inner Src: 10.0.1.5, Dst: 10.0.2.5)"| Node_2
    
    classDef NodeBox fill:#f9f9f9,stroke:#333,stroke-width:2px;
    class Node_1,Node_2 NodeBox;
```

## Slide 8

```mermaid
graph TB
    Client["Pod B<br>(client)"]
    Svc["Service (ClusterIP)<br>10.96.0.100"]
    P1["Pod A1"]
    P2["Pod A2"]

    Client --> Svc
    Svc --> P1
    Svc --> P2
```

## Slide 9 (eBPF)

```mermaid
flowchart LR
    subgraph IP["kube-proxy (iptables mode)"]
        direction TB
        i1(Service translation via netfilter rules)
        i2(DNAT to backend endpoint)
        i3("Connection tracking (conntrack)")
        i1 ~~~ i2 ~~~ i3
    end

    subgraph EB[eBPF]
        direction TB
        e1(Programmable datapath)
        e2(Service lookup via BPF maps)
        e3("Direct in-kernel forwarding (no rule traversal)")
        e1 ~~~ e2 ~~~ e3
    end

    IP -.- |Same Service semantics<br>different datapaths| EB

    style IP fill:#f2f2f2,stroke:#d9534f,stroke-width:2px,color:#333
    style EB fill:#e1f5fe,stroke:#03a9f4,stroke-width:2px,color:#333
```

## Slide 10

```mermaid
graph LR
    direction LR
    C[Pod B] --> N["Service datapath<br><i><small>(iptables or eBPF)</small></i>"]
    N --> P[Pod A]
    
    lbl1["Src: 10.244.2.8<br><span style='color:red'>Dst: 10.96.0.100 (Service IP)</span>"]
    lbl2["Src: 10.244.2.8<br><span style='color:green'>Dst: 10.244.1.5 (Pod IP)</span>"]

    C -.- lbl1
    N -.- lbl2

    classDef plainLabel fill:none,stroke:none;
    class lbl1,lbl2 plainLabel;

    %% Highlight the packet flow
    linkStyle 0,1 stroke:#ff5555,stroke-width:4px;
```

## Slide 12 (Tracing)

```mermaid
graph LR
    subgraph Logical ["Service Abstraction"]
        S1[Pod A]
        S2[Service IP]
        lbl1[dst = ClusterIP]
    end

    subgraph NodeDatapath ["Node 1 datapath"]
        S3["DNAT<br><i><small>(iptables or eBPF)</small></i>"]
        S4[routing<br>remote node]
        lbl2[dst = PodIP]
    end

    subgraph PhysicalDestination ["Remote Node"]
        S5[Node 2]
        S6[Pod B]
    end

    S1 --> S2
    S2 --> S3
    S3 --> S4
    S4 --> S5
    S5 --> S6

    S1 -.- lbl1
    S3 -.- lbl2

    classDef plainLabel fill:none,stroke:none;
    class lbl1,lbl2 plainLabel;
    %% Highlight the packet flow
    linkStyle 0,1,2,3,4 stroke:#ff5555,stroke-width:4px;
```

## Slide 12

```mermaid
%%{init: {'sequence': {'mirrorActors': false}}}%%
sequenceDiagram
    participant Pod as Pod B
    participant Svc as kube-dns<br><br>ClusterIP: 10.96.0.10
    participant CoreDNS as CoreDNS Pod

    Pod->>Svc: nslookup pod-a-svc
    Svc->>CoreDNS: Query forwarded
    CoreDNS-->>Pod: DNS response: 10.96.5.20 (Service IP)
```

## Slide 13 (old)

```mermaid
graph LR
    subgraph Node2 ["Node 2 - kubecon-worker"]
        PodB["Pod B (Client)"]
        KubeProxy2["kube-proxy"]
    end

    subgraph Node1 ["Node 1 - kubecon-control-plane"]
        SvcA["Service A"]
        PodA["Pod A (Server)"]
        KubeProxy1["kube-proxy"]
        CoreDNS1["coredns"]
        
        %% Relationships
        SvcA -.-> PodA
    end

    PodB --> SvcA
    PodB --> PodA

    linkStyle 1,2 stroke:#ff5555,stroke-width:4px;
```

## Slide 14

```mermaid
graph LR
    subgraph Node1 ["Node 'kubecon-worker2'"]
        PodA[Pod 'frontend'<br>eth0] --> Root1[host-side<br>veth]
        Root1 -->|routing| Phys1[eth0]
    end

    Phys1 -->|network| Phys2[eth0]

    subgraph Node2 ["Node 'kubecon-worker'"]
        Phys2 -->|routing| Root2[host-side<br>veth]
        Root2 --> PodB[Pod 'backend'<br>eth0]
    end

    %% Highlight the packet flow
    linkStyle 0,1,2,3,4 stroke:#ff5555,stroke-width:4px;
```

## Slide 16 NetworkPolicies

```mermaid
graph LR
    subgraph Node1 ["Node 'kubecon-worker'"]
        PodA[Pod 'backend'<br>eth0] --> Root1[host-side<br>veth]
        Root1 -->|routing| Phys1[eth0]
    end

    Phys1 -->|network| Phys2[eth0]

    subgraph Node2 ["Node 'kubecond-worker2'"]
        Phys2 -->|routing| NP{NetworkPolicy<br>iptables/eBPF}
        NP --x|DENY / DROP| Blocked((X))
        NP -.-> Root2[host-side<br>veth]
        Root2 -.-> PodB[Pod 'frontend'<br>eth0]
    end

    style Blocked fill:#f00,color:#fff,stroke:#a00,stroke-width:2px,font-weight:bold
    style NP fill:#fee,stroke:#f00,stroke-width:2px

    %% Highlight the packet flow
    linkStyle 0,1,2,3,4 stroke:#ff5555,stroke-width:4px;
```

## Demo Architecture

To make this tangible, let's visualize the environment we are debugging.

We have a 2-node cluster. Each node runs `kube-proxy`, and we have a central `coredns` service. We have two application workloads: **Pod A** (Client) on Node 1 and **Pod B** (Server) on Node 2. Both are exposed via Services.

```mermaid
graph TB
    subgraph K8s ["Kubernetes Cluster"]
        subgraph ControlPlane ["Control Plane Node"]
            direction TB
            Etcd["etcd (Store)"]
            ApiServer["API Server"]
            KCM["Controller Manager<br>(EndpointSlice Controller)"]
            
            subgraph Resources ["Service Discovery Resources"]
                SvcA["Service A<br>10.96.1.1"]
                EpA["EndpointSlice A"]
                
                SvcB["Service B<br>10.96.2.2"]
                EpB["EndpointSlice B"]
                
                KubeDNS["kube-dns<br>10.96.0.10"]
                EpDNS["EndpointSlice DNS"]
            end
        end

        subgraph Node1 ["Node 1 (10.0.0.10)"]
            KubeProxy1["kube-proxy"]
            Rules1["iptables/IPVS Rules"]
            
            subgraph DNSLogic ["DNS"]
                CoreDNS["coredns Pod<br>10.244.0.3"]
                DNSRecords["DNS Records<br>svc-a.default -> 10.96.1.1"]
            end

            PodA["Pod A (Client)<br>10.244.1.5"]
        end

        subgraph Node2 ["Node 2 (10.0.0.11)"]
            KubeProxy2["kube-proxy"]
            Rules2["iptables/IPVS Rules"]
            PodB["Pod B (Server)<br>10.244.2.8"]
        end

        %% Control Plane Logic
        Etcd <--> ApiServer
        ApiServer <--> KCM
        KCM -.->|Creates & Syncs| Resources

        %% Proxy Watches (Data Plane)
        KubeProxy1 -.->|Watches| ApiServer
        KubeProxy1 -->|Writes| Rules1
        
        KubeProxy2 -.->|Watches| ApiServer
        KubeProxy2 -->|Writes| Rules2

        %% Resource Linking (Logical)
        SvcA -.->|Selector| EpA 
        EpA -.->|Target| PodA
        
        SvcB -.->|Selector| EpB
        EpB -.->|Target| PodB
        
        KubeDNS -.->|Selector| EpDNS
        EpDNS -.->|Target| CoreDNS
    end
```
